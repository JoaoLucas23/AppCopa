import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 250,
    marginBottom: 5,
    backgroundColor: '#000',
    borderWidth: 2,
    borderColor: '#f234',
    borderRadius: 8,
    borderStyle: 'solid',
  }
});