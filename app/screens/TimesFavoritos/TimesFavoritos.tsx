import React from 'react';
import { View, SafeAreaView } from 'react-native';

import { styles } from './styles';

export function TimesFavoritos() {
  return (
    <SafeAreaView style={styles.container}>
        
    </SafeAreaView>
  );
}