import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  matches: {
    flex: 0.6,
    paddingTop: 10,
},
titles: {
    fontSize: 20,
    paddingHorizontal: 10,
    color: '#ffff',
    fontWeight: 'bold',
}
});