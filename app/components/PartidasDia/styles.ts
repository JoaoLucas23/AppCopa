import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  matches: {
    flex: 0.6,
    paddingTop: 5,
},
titles: {
    fontSize: 30,
    paddingHorizontal: 10,
    color: '#ffff',
    fontWeight: '900',
}
});